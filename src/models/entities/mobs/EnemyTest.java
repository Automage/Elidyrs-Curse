package models.entities.mobs;

import models.Sprite;

public class EnemyTest extends Mob {
    public EnemyTest(int x, int y, int speed, Sprite... sprites) {
        super(x, y, speed, sprites);
    }
}
